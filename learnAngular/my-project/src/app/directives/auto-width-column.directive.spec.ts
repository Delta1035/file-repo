import { AutoWidthColumnDirective } from './auto-width-column.directive';

describe('AutoWidthColumnDirective', () => {
  it('should create an instance', () => {
    // const directive = new AutoWidthColumnDirective();
    // expect(directive).toBeTruthy();
  });
});
