import { NgModelStatusDirective } from './ng-model-status.directive';

describe('NgModelStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModelStatusDirective();
    expect(directive).toBeTruthy();
  });
});
